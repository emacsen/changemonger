-extends "base.haml"
-set title = "Node details"
-block content:
  %h1 << Node {{element['id'] }}
  %h2 << {{ name }}
  %p << Object matched to
  %ul
    -for feature in features:
      %li << {{feature.name}} ({{precision(feature)}})
