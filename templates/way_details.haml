-extends "base.haml"
-set title = "Way details"
-block content:
  %h1 << Way {{way['id'] }}
  %h2 << {{ name }}
  %p << Object matched to
  %ul
    -for feature in features:
      %li << {{feature.name}} ({{feature.precision}})
